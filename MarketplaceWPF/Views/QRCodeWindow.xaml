<Window x:Class="MarketplaceWPF.Views.QRCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QR-код заказа" 
        Height="800" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Информация о заказе -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Заказ оформлен!" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <Border Background="#E3F2FD" 
                    Padding="15" 
                    CornerRadius="8"
                    Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock FontSize="14" Margin="0,0,0,5">
                        <Run Text="Номер заказа:" FontWeight="Bold"/>
                        <Run Text="{Binding OrderId, StringFormat='#{0}'}"/>
                    </TextBlock>

                    <TextBlock FontSize="14" Margin="0,0,0,5">
                        <Run Text="Сумма:" FontWeight="Bold"/>
                        <Run Text="{Binding TotalPrice}"/>
                        <Run Text=" руб."/>
                    </TextBlock>

                    <TextBlock FontSize="14" Margin="0,0,0,5">
                        <Run Text="Статус:" FontWeight="Bold"/>
                        <Run Text="{Binding StatusText}"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Background="#FFF3E0" 
                    Padding="15" 
                    CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="📍 Пункт выдачи" 
                               FontWeight="Bold" 
                               FontSize="14"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding PickupPointName}" 
                               FontSize="14"
                               Margin="0,0,0,3"/>
                    <TextBlock Text="{Binding PickupPointAddress}" 
                               FontSize="12" 
                               Foreground="Gray"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- QR-код -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#E0E0E0" 
                BorderThickness="2"
                CornerRadius="10"
                Padding="20">
            <StackPanel>
                <TextBlock Text="Покажите этот QR-код на ПВЗ" 
                           FontSize="14" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"/>

                <Image x:Name="QRCodeImage" 
                       Width="280" 
                       Height="280"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

                <TextBlock Text="{Binding QRCodeText}" 
                           FontSize="18" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Border>

        <!-- Кнопка закрыть -->
        <Button Grid.Row="2"
                Content="Закрыть"
                Height="45"
                Background="#2196F3"
                Foreground="White"
                FontSize="16"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Margin="0,20,0,0"
                Click="Close_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1976D2"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>