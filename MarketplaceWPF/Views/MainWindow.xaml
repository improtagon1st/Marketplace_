<Window x:Class="MarketplaceWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Маркетплейс" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Стиль для кнопок меню -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левое меню -->
        <Border Grid.Column="0" Background="#2196F3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Верхняя часть с меню -->
                <StackPanel Grid.Row="0">
                    <!-- Заголовок -->
                    <Border Background="#1976D2" Height="80" Padding="15">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="🛒 Маркетплейс" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                            <TextBlock x:Name="UserNameText"
                               FontSize="12" 
                               Foreground="White" 
                               Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Меню для Customer -->
                    <!-- Меню для Customer -->
                    <StackPanel x:Name="CustomerMenu" Visibility="Collapsed">
                        <Button Content="🏠 Каталог" Click="ShowCatalog_Click" x:Name="BtnCatalog" Style="{StaticResource MenuButtonStyle}"/>
                        <Button x:Name="BtnCart" Content="🛒 Корзина" Click="ShowCart_Click" Style="{StaticResource MenuButtonStyle}"/>
                        <Button x:Name="BtnOrders" Content="📦 Мои заказы" Click="ShowOrders_Click" Style="{StaticResource MenuButtonStyle}"/>
                        <Button x:Name="BtnProfile" Content="👤 Профиль" Click="ShowProfile_Click" Style="{StaticResource MenuButtonStyle}"/>
                    </StackPanel>

                    <!-- Меню для Worker -->
                    <StackPanel x:Name="WorkerMenu" Visibility="Collapsed">
                        <Button x:Name="BtnPickupOrders"
                            Content="📋 Заказы на ПВЗ" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Click="ShowPickupOrders_Click"/>
                        <Button x:Name="BtnScanQR"
                            Content="🔍 Сканировать QR" 
                            Style="{StaticResource MenuButtonStyle}" 
                            Click="ShowScanQR_Click"/>
                    </StackPanel>

                    <StackPanel x:Name="AdminMenu" Visibility="Collapsed">
                                    <Button x:Name="BtnProducts"
                        Content="📦 Управление товарами" 
                        Style="{StaticResource MenuButtonStyle}" 
                        Click="ShowProducts_Click"/>
                                    <Button x:Name="BtnCategories"
                        Content="📂 Категории" 
                        Style="{StaticResource MenuButtonStyle}" 
                        Click="ShowCategories_Click"/>
                                    <Button x:Name="BtnPickupPoints"
                        Content="📍 Пункты выдачи" 
                        Style="{StaticResource MenuButtonStyle}" 
                        Click="ShowPickupPoints_Click"/>
                        <Button x:Name="BtnWorkers" Content="👥 Работники" Click="ShowWorkers_Click" Style="{StaticResource MenuButtonStyle}"/>
                                    <Button x:Name="BtnAllOrders"
                        Content="📊 Все заказы" 
                        Style="{StaticResource MenuButtonStyle}" 
                        Click="ShowAllOrders_Click"/>
                                </StackPanel>
                </StackPanel>

                <!-- Кнопка выхода внизу -->
                <Button Grid.Row="1"
                Content="🚪 Выход" 
                Style="{StaticResource MenuButtonStyle}" 
                Click="Logout_Click"/>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Frame x:Name="ContentFrame" 
               Grid.Column="1" 
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>