<Page x:Class="MarketplaceWPF.Views.PickupPointOrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:MarketplaceWPF.ViewModels"
      xmlns:local="clr-namespace:MarketplaceWPF.Helpers"
      Title="PickupPointOrdersPage">

    <Page.Resources>
        <!-- Стиль для RadioButton фильтров -->
        <Style x:Key="FilterRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="20"
                                Padding="15,8">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,20">
            <TextBlock Text="📋 Заказы на ПВЗ" 
                       FontSize="28" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <Button Content="🔄 Обновить"
                    Command="{Binding RefreshCommand}"
                    Height="40"
                    Margin="20,0,0,0"
                    Padding="15,0"
                    Background="#2196F3"
                    Foreground="White"
                    FontSize="14"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Фильтры -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,20">
            

            <RadioButton Content="⏳ Ожидают доставки" 
             GroupName="FilterGroup"
             Tag="Created"
             Checked="Filter_Changed"
             Style="{StaticResource FilterRadioButtonStyle}"
             Margin="10,0,0,0"/>

            <RadioButton Content="📦 Готовы к выдаче" 
             GroupName="FilterGroup"
             Tag="Delivered"
             Checked="Filter_Changed"
             Style="{StaticResource FilterRadioButtonStyle}"
             Margin="10,0,0,0"/>

            <RadioButton Content="✅ Выданные" 
             GroupName="FilterGroup"
             Tag="PickedUp"
             Checked="Filter_Changed"
             Style="{StaticResource FilterRadioButtonStyle}"
             Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Список заказов -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Orders}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" 
                                BorderBrush="#E0E0E0" 
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,15"
                                Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Заголовок заказа -->
                                <Grid Grid.Row="0" Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontSize="20" FontWeight="Bold">
                                            <Run Text="Заказ #"/>
                                            <Run Text="{Binding Id}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                   FontSize="12" 
                                                   Foreground="Gray"
                                                   Margin="0,3,0,0"/>
                                    </StackPanel>

                                    <!-- Статус -->
                                    <Border Grid.Column="1"
                                            Height="30"
                                            CornerRadius="15"
                                            Padding="15,0"
                                            VerticalAlignment="Center"
                                            Margin="0,0,15,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Created">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Delivered">
                                                        <Setter Property="Background" Value="#E3F2FD"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="PickedUp">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock VerticalAlignment="Center"
                                                   FontSize="12"
                                                   FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Created">
                                                            <Setter Property="Text" Value="⏳ Ожидает"/>
                                                            <Setter Property="Foreground" Value="#F57C00"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Delivered">
                                                            <Setter Property="Text" Value="📦 На ПВЗ"/>
                                                            <Setter Property="Foreground" Value="#1976D2"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="PickedUp">
                                                            <Setter Property="Text" Value="✅ Выдан"/>
                                                            <Setter Property="Foreground" Value="#388E3C"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>

                                    <!-- Код заказа -->
                                    <Border Grid.Column="2"
                                            Background="#F5F5F5"
                                            CornerRadius="5"
                                            Padding="10,5">
                                        <TextBlock Text="{Binding QRCode}" 
                                                   FontFamily="Consolas"
                                                   FontSize="14"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>

                                <!-- Информация -->
                                <Grid Grid.Row="1" Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Клиент -->
                                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                        <TextBlock Text="👤 Клиент" 
                                                   FontSize="12" 
                                                   Foreground="Gray"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding CustomerName}" 
                                                   FontSize="14" 
                                                   FontWeight="Bold"/>
                                        <TextBlock Text="{Binding CustomerPhone}" 
                                                   FontSize="12" 
                                                   Foreground="Gray"/>
                                    </StackPanel>

                                    <!-- Товары -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="📦 Товары" 
                                                   FontSize="12" 
                                                   Foreground="Gray"
                                                   Margin="0,0,0,5"/>
                                        <ItemsControl ItemsSource="{Binding Items}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock FontSize="12">
                                                        <Run Text="{Binding ProductName}"/>
                                                        <Run Text=" ×"/>
                                                        <Run Text="{Binding Quantity}" FontWeight="Bold"/>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Grid>

                                <!-- Сумма и кнопка -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               FontSize="18" 
                                               FontWeight="Bold"
                                               VerticalAlignment="Center">
                                        <Run Text="Итого:"/>
                                        <Run Text="{Binding TotalPrice}" Foreground="Green"/>
                                        <Run Text=" руб." Foreground="Green"/>
                                    </TextBlock>

                                    <!-- Кнопка отметить доставленным -->
                                    <Button Grid.Column="1"
                                            Content="✓ Доставлен в ПВЗ"
                                            Command="{Binding DataContext.MarkDeliveredCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Width="180"
                                            Height="40"
                                            Background="#4CAF50"
                                            Foreground="White"
                                            FontSize="14"
                                            FontWeight="Bold"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="8">
                                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Created">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#388E3C"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

   
</Page>