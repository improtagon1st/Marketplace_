<Window x:Class="MarketplaceWPF.Views.WorkerEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление работником" 
        Height="500" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   x:Name="TitleText"
                   Text="Добавить работника" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Форма -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Email -->
                <TextBlock Text="Email *" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="EmailInput"
                         FontSize="14"
                         Height="40"
                         Padding="10"
                         Margin="0,0,0,15"/>

                <!-- Пароль -->
                <TextBlock x:Name="PasswordLabel"
                           Text="Пароль *" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordInput"
                             FontSize="14"
                             Height="40"
                             Padding="10"
                             Margin="0,0,0,5"/>
                <TextBlock x:Name="PasswordHint"
                           Text="Оставьте пустым, чтобы не менять пароль"
                           FontSize="12"
                           Foreground="Gray"
                           Margin="0,0,0,15"
                           Visibility="Collapsed"/>

                <!-- ФИО -->
                <TextBlock Text="ФИО *" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="FullNameInput"
                         FontSize="14"
                         Height="40"
                         Padding="10"
                         Margin="0,0,0,15"/>

                <!-- Телефон -->
                <TextBlock Text="Телефон *" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="PhoneInput"
                         FontSize="14"
                         Height="40"
                         Padding="10"
                         Margin="0,0,0,15"/>

                <!-- Пункт выдачи -->
                <TextBlock Text="Пункт выдачи" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <ComboBox x:Name="PickupPointCombo"
                          DisplayMemberPath="Name"
                          FontSize="14"
                          Height="40"
                          Margin="0,0,0,5"/>
                <TextBlock Text="Можно оставить без привязки к ПВЗ"
                           FontSize="12"
                           Foreground="Gray"
                           Margin="0,0,0,15"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Отмена"
                    Width="100"
                    Height="45"
                    Margin="0,0,15,0"
                    Background="Transparent"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    FontSize="14"
                    Cursor="Hand"
                    Click="Cancel_Click"/>

            <Button Content="Сохранить"
                    Width="150"
                    Height="45"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Save_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>