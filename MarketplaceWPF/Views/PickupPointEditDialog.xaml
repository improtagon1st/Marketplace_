<Window x:Class="MarketplaceWPF.Views.PickupPointEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование ПВЗ" 
        Height="550" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   x:Name="TitleText"
                   Text="Добавить ПВЗ" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,30"/>

        <!-- Форма -->
        <StackPanel Grid.Row="1">
            <!-- Название -->
            <TextBlock Text="Название ПВЗ" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="NameBox"
                     FontSize="14"
                     Height="40"
                     Padding="10"
                     Margin="0,0,0,15"/>

            <!-- Адрес -->
            <TextBlock Text="Адрес" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="AddressBox"
                     FontSize="14"
                     Height="40"
                     Padding="10"
                     Margin="0,0,0,15"/>

            <!-- Телефон -->
            <TextBlock Text="Телефон" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="PhoneBox"
                     FontSize="14"
                     Height="40"
                     Padding="10"
                     Margin="0,0,0,15"/>

            <!-- Время работы -->
            <TextBlock Text="Часы работы" 
                       FontSize="14" 
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="WorkingHoursBox"
                     FontSize="14"
                     Height="40"
                     Padding="10"
                     Margin="0,0,0,15"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Отмена"
                    Width="100"
                    Height="45"
                    Margin="0,0,15,0"
                    Background="Transparent"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    FontSize="14"
                    Cursor="Hand"
                    Click="Cancel_Click"/>

            <Button Content="Сохранить"
                    Width="120"
                    Height="45"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Save_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>